# 시험 결과

`npm test` 가 적는다. 손으로 고치지 않는다.

- 돌린 때 2026. 9. 11. 오후 6:14:02
- 버전 v2.14.0-3-g059e0ff (059e0ff)
- **401가지 중 401가지 통과**

| 자리 | 무엇을 보나 | 결과 |
| --- | --- | --- |
| `test/world.mjs` 판 안의 규칙 | 메뉴 · 화면 고르기 · 붙잡기 · 뿌리치기 · 투명도 | 104/104 |
| `test/volley.mjs` 배구 | 편 · 점수 · 벽 · 바닥 · 천장 · 슬라이딩 | 88/88 |
| `test/net.mjs` 여럿이 붙잡기 | 뭉쳐 있을 때 손이 몇 개나 가나 | 17/17 |
| `test/switch.mjs` 게임 갈아 끼우기 | 똥피하기를 하다 배구방에 들어가기 | 12/12 |
| `test/edge.mjs` 경우의 수 ①~⑭ | 나가기 · 구경 · 깨진 꾸러미 · 판 크기 | 24/24 |
| `test/edge2.mjs` 경우의 수 ⑮~㉖ | 동시에 붙잡기 · 한 편이 통째로 나가기 · 오래 굴리기 | 60/60 |
| `test/coop.mjs` 넷이서 | 타일 · 점프 · 사다리 · 상자 · 열쇠 · 누름판 · 포탈 · 출구 · 카메라 · 꾸러미 | 96/96 |

## 판 안의 규칙

**메뉴 — 첫 화면은 여섯 줄이다**

- 항목 → `["이어서 하기","같이 하기","화면","홈으로 나가기","화면 숨기기","게임 끝내기"]`
- 지금 혼자인지 옆에 적는다 → `"혼자 하는 중"`
- 창을 어떻게 띄워 뒀는지도 → `"화면 전체"`
- 무슨 게임 중인지도 → `"똥피하기"`

**메뉴 — 판을 하는 중이면 「다시 시작」이 붙는다**

- 둘째 줄 → `"다시 시작"`

**메뉴 — 게임 도중 홈으로 나간다**

- 게임 고르는 화면으로 → `"pick"`
- 메뉴는 닫힌다 → `false`
- 고르는 줄은 하던 게임에 가 있다 → `0`
- 홈 메뉴 → `["고르던 데로","같이 하기","화면","화면 숨기기","게임 끝내기"]`

**같이 하기 — 방을 열 때 무슨 게임인지부터 고른다**

- 안으로 들어왔다 → `["together"]`
- 고를 것 → `["방 만들기","코드로 입장","이름 바꾸기"]`
- 또 한 겹 → `["together","host"]`
- 게임 목록 → `["똥피하기","배구","넷이서"]`
- 처음 짚는 것은 하던 게임 → `0`
- 셸로 넘어간 것 → `["host:volley"]`
- 메뉴는 닫힌다 → `false`

**같이 하기 — 방 안에서는 코드 복사와 닫기**

- 방 이름이 옆에 뜬다 → `"방 K3P9 · 1명"`
- 고를 것 → `["이름 바꾸기","코드 복사","방 깨기 — 모두 홈으로"]`
- 코드가 옆에 → `"K3P9"`
- 손님은 나가기 → `"방에서 나가기"`

**이름 바꾸기 — 메뉴에 있다**

- 혼자일 때도 고칠 수 있다 → `["방 만들기","코드로 입장","이름 바꾸기"]`
- 셸이 이름 묻는 창을 연다 → `["name"]`
- 메뉴는 닫힌다 → `false`

**내보내기 — 방장만, 그 자리에서**

- 방장에게는 내보내기가 있다 → `["이름 바꾸기","코드 복사","내보내기","방 깨기 — 모두 홈으로"]`
- 몇 명인지 옆에 → `"2명"`
- 사람 목록 → `["범창","잠수"]`
- 뭐 하고 있는지도 → `["하는 중","구경 중"]`
- 셸로 넘어간 것 → `["kick:3"]`
- 목록에 남아 있다 (둘째도 내보낼 수 있다) → `[true,["together","kick"]]`
- 다 내보내면 한 겹 나온다 → `["이름 바꾸기","코드 복사","방 깨기 — 모두 홈으로"]`
- 길도 나왔다 → `["together"]`

**내보내기 — 손님은 못 한다**

- 나가기만 있다 → `["이름 바꾸기","코드 복사","방에서 나가기"]`

**화면 — 크기·자리·투명도·띄울 화면이 한 겹 안에 모인다**

- 화면 전체면 「창 위치」가 없다 → `["창 크기","투명도","⌥ 떼면 숨기기","⌥ 고정 — 방향키만으로"]`
- 두 겹 안 → `["screen","size"]`
- 고를 것들 → `["화면 전체","3/4","절반","작게","아주 작게"]`
- 지금 것에 점 → `[true,false,false,false,false]`
- 셸로 넘어간 것 → `["size:0.55"]`
- 고르고도 열려 있다 → `[true,["screen","size"]]`
- 한 겹 뒤로 → `["screen"]`
- 줄이고 나면 「창 위치」가 생긴다 → `["창 크기","창 위치","투명도","⌥ 떼면 숨기기","⌥ 고정 — 방향키만으로"]`
- 첫 줄 옆에 지금 상태 → `"절반"`
- 놓을 자리 → `["정중앙","왼쪽 위","오른쪽 위","왼쪽 아래","오른쪽 아래"]`
- 셸로 넘어간 것 → `["size:0.55","spot:tr"]`
- 첫 화면까지 나왔다 → `[]`
- 한 번 더 누르면 닫힌다 → `false`

**⌥ 떼면 숨기기 — 그 자리에서 켜고 끈다**

- 처음엔 켜져 있다 → `["켜짐",true]`
- 셸로 넘어간 것 → `["peek:0"]`
- 메뉴는 그대로 열려 있다 → `[true,["screen"]]`
- 꺼진 것이 보인다 → `["꺼짐",false]`
- 다시 켜는 값이 넘어간다 → `["peek:0","peek:1"]`

**화면 — 모니터가 여럿이면 「띄울 화면」이 생긴다**

- 다섯 줄 → `["창 크기","투명도","⌥ 떼면 숨기기","⌥ 고정 — 방향키만으로","띄울 화면"]`
- 지금 어디에 떠 있는지 옆에 적는다 → `"Built-in Retina Display"`
- 목록 → `["Built-in Retina Display","DELL U2723QE","LG UltraFine"]`
- 해상도도 같이 → `["1512×982","2560×1440","3840×2160"]`
- 지금 화면에 표시 → `[true,false,false]`
- 처음 짚는 것은 지금 화면 → `0`
- 셸로 넘어간 것 → `["screen:2"]`
- 고르고도 메뉴는 열려 있다 → `[true,["screen","where"]]`

**화면 — 고르는 중에 모니터를 뽑으면 한 겹 나온다**

- 목록이 아니라 화면 설정으로 → `["창 크기","투명도","⌥ 떼면 숨기기","⌥ 고정 — 방향키만으로"]`
- 길도 한 겹 나왔다 → `["screen"]`

**투명도 — 화면 안에서 고르고 셸로 넘어간다**

- 목록 → `["그대로","85%","70%","55%","40%"]`
- 셸로 넘어간 것 → `["fade:0.7"]`

**메뉴 — 화면 목록이 판을 다시 시작해도 안 사라진다**

- 남아 있다 → `3`

**붙잡기 — 누르고 있는 동안 잡는다**

- 잡았다 → `2`
- 1초 뒤에도 잡고 있다 → `2`
- 떼면 놓는다 → `-1`

**붙잡기 — 예전처럼 4초에 저절로 풀리지 않는다**

- 안 죽었다 → `false`
- 6초 뒤에도 잡고 있다 → `2`

**붙잡기 — 더 가까이 붙는다**

- 간격이 21픽셀 언저리 → `21`
- 예전 32보다 붙었다 → `true`

**붙잡기 — 잡힌 쪽이 스페이스바로 뿌리친다**

- 잡혔다 → `2`
- 풀렸다 → `-1`
- 뿌리친 횟수가 늘었다 → `1`

**뿌리치기 — 밀쳐 내면서 풀려난다 (잡힌 쪽)**

- 왼쪽으로 밀려난다 → `true`
- 실제로 물러났다 → `true`
- 0.2초면 3분의 1 밑으로 잦아든다 → `true`

**뿌리치기 — 반대쪽에서 잡혔으면 반대로 밀려난다**

- 오른쪽으로 밀려난다 → `true`

**뿌리치기 — 붙잡고 있던 쪽이 뒤로 밀린다**

- 내가 잡았다 → `2`
- 손이 풀렸다 → `-1`
- 왼쪽으로 밀려난다 → `true`
- 잡힌 쪽보다 세게 밀린다 → `true`
- 실제로 물러났다 → `true`
- 팔 방향도 풀렸다 → `0`

**뿌리치기 — 밀려나도 벽은 안 뚫는다**

- 벽 안에 남는다 → `true`
- 벽에 닿으면 밀려남이 멈춘다 → `0`

**뿌리치기 — 밀려남이 영영 남지 않는다**

- 1초 뒤엔 0 → `0`

**붙잡기 — 오른쪽을 잡고 왼쪽으로 가면 팔은 오른쪽에 남는다**

- 걸음은 왼쪽 → `-1`
- 팔은 오른쪽 → `1`
- 실제로 왼쪽으로 갔다 → `true`
- 놓으면 팔 방향도 풀린다 → `0`

**붙잡기 — 남이 남을 잡은 것도 보인다**

- 3번은 붙잡힌 상태로 그려진다 → `2`
- 2번의 팔은 3번 쪽(오른쪽) → `1`
- 3번의 팔은 2번 쪽(왼쪽) → `-1`
- 나는 아무와도 안 엮였다 → `[-1,-1]`

**붙잡기 — 메뉴를 열었다 닫아도 손이 안 굳는다**

- 놓였다 → `-1`

**게임 고르기 — 배구로 갈아 끼우면 살림살이도 바뀐다**

- 켜면 고르는 화면 → `"pick"`
- 배구 → `"volley"`
- 공이 생겼다 → `"object"`
- 점수 0:0 → `[0,0]`
- 메뉴에 지금 게임이 적힌다 → `"배구"`
- 다시 고르는 화면 → `"pick"`

**붙잡기 — 누르고 있으면 다가오는 사람을 잡는다**

- 아직 못 잡는다 → `-1`
- 다가오자 잡혔다 → `2`
- 떼면 놓는다 → `-1`

## 배구

**편 — 네트를 넘어갈 수 없다**

- 네트 앞에서 멈춘다 → `true`
- 넘어가지 않았다 → `742`
- 반대쪽도 마찬가지 → `770`

**편 — 판이 끝나도 못 넘는다**

- 시작 전에도 못 넘는다 → `true`

**편 — 메뉴에서 골라 옮긴다**

- 메뉴에 편 고르기가 있다 → `true`
- 지금 편이 옆에 적힌다 → `"빨강"`
- 안으로 들어왔다 → `["team"]`
- 목록 → `["빨강 편","파랑 편"]`
- 지금 편에 점 → `[true,false]`
- 편이 바뀌었다 → `1`
- 오른쪽 코트로 옮겨졌다 → `true`
- 고르고도 메뉴는 열려 있다 → `[true,["team"]]`
- 이제 파랑에 점 → `[false,true]`

**편 — 다시 시작해도 편은 남는다**

- 그대로 파랑 → `1`
- 오른쪽에 선다 → `true`

**편 — 옷 색이 편을 따라간다**

- 왼쪽은 빨강 → `"#b5352f"`
- 오른쪽은 파랑 → `"#2f6fb0"`

**점수 — 우리 쪽에 떨어지면 상대 점수**

- 왼쪽에 떨어지면 그 즉시 파랑 득점 → `[0,1]`
- 오른쪽에 떨어지면 그 즉시 빨강 득점 → `[1,1]`
- 바로 다음 서브를 기다린다 → `true`

**벽 — 입사각 그대로 반사각**

- 튕기긴 했다 → `true`
- 세로 속도는 거의 그대로 (중력 한 프레임분만 차이) → `true`
- 가로만 뒤집혔다 → `true`
- 입사각 = 반사각 (2도 안쪽) → `true`
- 벽 안쪽에 있다 → `true`

**벽 — 아주 빠른 공도 벽에 안 붙는다**

- 한자리에 머문 프레임 없음 → `0`
- 벽을 뚫지 않았다 → `true`
- 되돌아 나왔다 → `true`

**듀스 — 다섯 점 먼저, 단 두 점 차로**

- 4:4 는 듀스 → `true`
- 4:3 은 아직 듀스 아님 → `false`
- 5:4 도 듀스 (두 점 차가 안 났다) → `true`
- 5:3 은 끝 → `true`
- 5:4 는 안 끝 → `false`
- 6:4 는 끝 → `true`
- 7:6 은 안 끝 → `false`
- 8:6 은 끝 → `true`
- 0:5 는 끝 → `true`
- 5:4 에서는 판이 안 끝난다 → `0`
- 점수가 그대로다 → `"5:4"`
- 6:4 가 되면 끝난다 → `1`
- 빨강 편이 이겼다 → `0`
- 순위표에 점수가 실린다 → `6`

**바닥 — 닿는 즉시 끝**

- 점수가 올라갔다 → `1`
- 바닥에 닿은 그 프레임에 끝났다 → `true`
- 바닥 밑으로 안 내려갔다 → `true`

**슬라이딩 — 손이 안 닿으면 몸을 던진다**

- 미끄러지기 시작했다 → `true`
- 공 쪽으로 → `1`
- 실제로 미끄러져 갔다 → `true`
- 아직 미끄러지는 중 → `true`
- 도중에 방향을 못 튼다 → `true`
- 끝났다 → `0`
- 끝나면 쉰다 → `true`
- 쉬는 동안엔 또 못 던진다 → `0`

**슬라이딩 — 닿는 거리면 던지지 말고 쳐야 한다**

- 안 미끄러진다 → `0`
- 공이 위로 떴다 → `true`

**한쪽 편이 비면 판이 안 열린다**

- 나 혼자 빨강 → 못 연다 → `"파랑 편에 아무도 없다"`
- 2대1 이 아니라 1대1 → 열린다 → `null`
- 2대1 도 열린다 → `null`
- 편 이름표 → `true`
- 파랑이 비면 다시 막힌다 → `"파랑 편에 아무도 없다"`

**천장 — 위로 안 벗어난다**

- 화면 위로 안 나간다 → `true`
- 낮춰 둔 천장 위로는 안 간다 → `true`
- 그렇다고 네트 높이에서 놀지도 않는다 → `true`
- _가장 높이 올라간 자리 y=399 (천장 300, 바닥 918)_

**슬라이딩 — 방향키가 방향을 정한다**

- 공이 오른쪽, 키 안 잡음 → 오른쪽 → `1`
- 공이 왼쪽, 키 안 잡음 → 왼쪽 → `-1`
- 공이 오른쪽인데 ⌥← 를 잡음 → 왼쪽 → `-1`
- 공이 왼쪽인데 ⌥→ 를 잡음 → 오른쪽 → `1`

**스파이크 — 공보다 높이 떠서 때려야 꽂힌다**

- 손 높이의 공은 수평 미사일 → `true`
- 앞으로 나간다 → `true`
- 손보다 한참 밑이면 아래로 꽂힌다 → `true`
- 꽂을수록 가로는 준다 → `true`
- _손 높이 vy=0 · 손 밑 vy=1499_
- 중간 높이는 중간 각도 → `true`
- 낮게 뛰면 덜 세다 → `true`
- ⌥↓ 는 손 위의 공도 꽂는다 → `true`
- ⌥↑ 는 밑에서 때려도 올려 준다 → `true`
- 때린 사람 팔이 돈다 → `true`
- 땅에서 때리면 위로 올라간다 → `true`
- 땅에서는 팔이 안 돈다 → `0`

**공기 저항 — 세게 때린 공만 눈에 띄게 잦아든다**

- 강타는 반 초에 1할 넘게 준다 → `true`
- 살살 올린 공은 거의 그대로 → `true`
- 빠를수록 많이 깎인다 → `true`
- _반 초 뒤 남은 속도 — 강타 87% · 토스 97%_

**슬라이딩 게이지 — 비었다가 꽉 차면 또 된다**

- 평소에는 꽉 차 있다 → `1`
- 던지자마자 텅 빈다 → `0`
- 미끄러지는 동안은 계속 비어 있다 → `true`
- 그 뒤로는 줄지 않고 찬다 → `true`
- 중간에 반쯤 찬 구간이 있다 → `true`
- 결국 꽉 찬다 → `1`
- _비운 뒤 0.97초, 실제로 0.98초 만에 꽉 참_
- 꽉 찬 것과 실제로 되는 것이 한 프레임도 안 어긋난다 → `0`

## 여럿이 붙잡기

**뭉쳐 있을 때 한 번에 몇 명을 잡나**

- 한 명만 잡는다 → `1`
- 제일 가까운 사람(4번, 15px)을 잡는다 → `4`
- 놓자마자 다시 누르면 안 잡힌다 (0.7초 쉼) → `-1`
- 0.75초 지나면 다시 잡힌다 → `true`
- 그래도 한 명뿐 → `"number"`

**둘이 한 사람을 잡고 있다가 뿌리쳐지면 — 잡은 쪽 둘 다 놓는다**

- 내가 3번을 잡았다 → `3`
- 내 손이 풀렸다 → `-1`
- 내가 뒤로 밀려난다 → `true`
- 다시 잡기까지 쉬어야 한다 → `true`

**여러 명이 같은 한 사람을 동시에 잡을 수는 있나**

- 나는 잡힌 상태다 → `true`
- 30프레임 뒤에도 잡혀 있다 → `true`
- 둘 사이에 끼여 거의 못 움직인다 → `true`
- 한 번에 풀린다 → `-1`
- 뿌리침 횟수 +1 → `1`
- 밀려난다 → `true`

**내가 잡고 있는 동안 또 누가 붙어도 내 손은 한 명뿐**

- 계속 같은 사람 → `2`
- 바뀌지 않는다 → `2`

## 게임 갈아 끼우기

**배구방에 들어가기 전**

- 게임 → `"dodge"`
- 판이 돌고 있다 → `"play"`
- 똥이 떠 있다 → `true`

**방에 들어간 직후**

- 손님이 됐다 → `"guest"`

**방장 꾸러미를 한 번 받은 뒤**

- 배구로 갈아탔다 → `"volley"`
- 똥은 사라졌다 → `undefined`
- 공이 생겼다 → `"object"`
- 방장 점수를 받았다 → `[2,1]`
- 내 편도 받았다 → `1`
- 공이 움직인다 → `true`
- 바로 판을 본다 (구경) → `"play"`
- 이번 판에는 안 낀다 → `[true,true]`

## 경우의 수 ①~⑭

**① 방을 나가면 혼자로 돌아온다**

- 배구 중 → `true`
- 혼자로 → `false`
- 탈 안 남 → `true`

**② 방장이 판 도중에 게임을 바꾸면 손님도 따라온다**

- 똥피하기 구경 중 → `true`
- 배구로 따라왔다 → `"volley"`
- 공이 성하다 → `true`

**③ 구경하는 사람은 공을 못 친다**

- 공이 그대로 → `[0,0]`

**④ 죽은 사람은 슬라이딩도 못 한다**

- 안 미끄러진다 → `0`

**⑤ 똥피하기에서는 편 고르기가 안 보인다**

- 편 고르기 없음 → `true`
- 배구에는 있음 → `true`

**⑥ 하위 메뉴를 보는 중에 목록이 줄어도 안 터진다**

- 목록이 아니라 화면 설정으로 → `true`
- 안 터진다 → `true`

**⑦ 판을 다시 시작해도 편·투명도·화면·게임이 남는다**

- 편 → `1`
- 투명도 → `0.7`
- 화면 목록 → `1`
- 게임 → `"volley"`

**⑧ 배구에서 붙잡기 키가 붙잡기로 새지 않는다**

- 아무도 안 잡았다 → `-1`

**⑨ 공이 네트 꼭대기 선에 정확히 떨어져도 점수가 갈린다**

- 한쪽이 점수를 얻었다 → `true`

**⑩ 사람이 없는 방에서 판이 끝나도 안 터진다**

- 혼자면 판을 안 끝낸다 → `true`

**⑪ 창을 숨겼다 켜도 입력이 안 굳는다**

- 달리는 중 → `true`
- 키가 풀렸다 → `false`

**⑫ 판 크기가 바뀌어도 사람이 화면 밖으로 안 나간다**

- 안쪽에 있다 → `true`

**⑬ 배구에서 판 크기가 바뀌어도 코트를 안 벗어난다**

- 오른쪽 코트 안 → `true`

**⑭ 손님이 깨진 꾸러미를 받아도 안 터진다**

- 살아 있다 → `true`

## 경우의 수 ⑮~㉖

**⑮ 서로 동시에 붙잡으면**

- 둘 다 잡은 상태로 안 엉킨다 → `true`

**⑯ 잡고 있던 사람이 방을 나가면**

- 잡았다 → `2`
- 손이 풀렸다 → `-1`
- 안 터진다 → `true`

**⑰ 잡고 있는데 내가 죽으면**

- 손이 풀렸다 → `-1`

**⑱ 배구 도중 한 편이 통째로 나가면**

- 처음엔 양쪽에 사람이 있다 → `true`
- 한쪽이 비었다고 말한다 → `true`
- 이긴 편 없이 끝난다 → `null`
- 빈 코트로 계속 돌지 않고 판을 접는다 → `true`
- 점수를 쌓지 않았다 → `true`
- 그래도 안 터진다 → `true`

**⑲ 게임을 빠르게 여러 번 갈아 끼워도**

- 살아 있다 → `true`
- 살림살이가 게임과 맞다 → `true`

**⑳ 공이 한 프레임에 두 사람에게 닿으면**

- 한 번만 튕긴다 (위로) → `true`
- 말도 안 되게 빠르지 않다 → `true`

**㉑ 판 크기를 모르는 채로 굴려도**

- 안 터진다 → `true`
- 그 뒤에도 성하다 → `true`

**㉒ 내 번호가 편 명단에 없으면**

- 편이 성한 값 → `true`
- 안 터진다 → `true`

**㉓ 슬라이딩으로 벽에 박으면**

- 벽 안쪽 → `true`
- 미끄러짐이 끝났다 → `true`

**㉔ 아주 오래 굴려도 (시계·자국)**

- 시계가 성하다 → `true`
- 자국이 안 쌓인다 → `true`

**㉕ 방장이 혼자일 때 판 끝내기**

- 끝났다 → `"over"`
- 세리머니가 돈다 → `true`

**㉘ 방 안에서 이름을 고치면**

- 이름만 바뀐다 → `true`
- 구경으로 밀려나지 않는다 → `true`
- 방장에게 알린다 → `true`
- 이름표가 바뀐다 → `true`
- 머리 위 이름도 바뀐다 → `true`
- 다음 스냅샷에 다시 실린다 → `true`
- 빈 이름은 무시한다 → `true`

**㉗ 방장이 나를 내보내면**

- 방을 나간다 → `true`
- 왜 나갔는지 뜬다 → `true`
- 몇 초 뒤에는 사라진다 → `true`

**㉖ 고르는 화면에서 메뉴를 열고 게임 바꾸기**

- 고르는 화면 → `"pick"`
- 메뉴가 열린다 → `true`
- 방 만들기가 없다 → `true`
- 코드로 입장이 없다 → `true`
- 게임 바꾸기도 없다 (이미 고르는 중) → `true`
- 끝내기는 있다 → `true`
- 닫힌다 → `true`
- 여전히 고르는 화면 → `"pick"`

**㉘ 손님끼리 — 한 사람이 나가면 다른 손님 화면에서도 사라진다**

- 방장과 둘째가 보인다 → `true`
- 내 몸은 남으로 안 만든다 → `true`
- 이름표도 붙었다 → `"둘째"`
- 둘째가 사라졌다 (가만히 선 채로 남지 않는다) → `true`
- 이름표도 지웠다 → `true`
- 방장은 그대로 → `true`
- pl 없는 스냅샷으로는 안 지운다 → `true`
- 안 터진다 → `true`

**㉙ 방장이 방을 깨면 — 손님은 방을 나가고, 방을 나온 사람은 홈으로**

- 방을 나간다 → `1`
- 왜 나갔는지 뜬다 → `true`
- 홈(게임 고르기)으로 나왔다 → `"pick"`
- 하던 게임에 손가락이 놓여 있다 → `"volley"`
- 메뉴는 닫혀 있다 → `true`
- 남들은 지워졌다 → `true`
- 방장은 bye 를 무시한다 → `0`

**㉚ 방을 나온 사람이 마침 준비 화면이었어도 홈으로**

- 홈으로 → `"pick"`
- 처음 켤 때의 off 는 그대로 준비 화면 → `"ready"`

## 넷이서

**판 — 열둘이 다 열리고 크기가 맞다**

- 판 수 → `12`
- 열둘 다 열렸다 → `true`
- 첫 판은 뒷마당 표지판 → `["뒷마당","표지판"]`
- 내 발이 시작 줄에 선다 → `18`
- 시작 자리 칸 → `20`

**걷기 — 벽에 막히고, 구멍은 뛰어 넘고, 떨어진다**

- 오른콍으로 걸었다 → `true`
- 첫 단(16칸) 앞에 막혔다 → `true`
- 벽에 붙어 더 못 간다 → `true`
- 땅에 서 있다 → `true`

**점프 — 두 칸은 넘고 세 칸은 못 넘는다**

- _점프 꼭대기 2.15칸_
- 두 칸은 넘는다 → `true`
- 세 칸은 못 넘는다 → `true`
- _뛰어서 옆으로 4.49칸_
- 네 칸은 간다 → `true`

**웅크리기 — 한 칸 굴을 지난다**

- 서서는 굴에 못 들어간다 → `true`
- 웅크렸다 → `true`
- 웅크려서 굴을 지난다 → `true`

**사다리 — ⌥↑ 로 오르고 좌우를 누르면 놓는다**

- 사다리에 붙었다 → `true`
- 2층에 올라섰다 → `true`
- 좌우를 누르면 사다리를 놓는다 → `true`

**가시 — 닿으면 죽고 시작 자리에서 다시 선다**

- 꺼진 다리에서 떨어져 가시에 닿으면 죽는다 → `true`
- 죽음을 센다 → `1`
- 0.8초 뒤 다시 선다 → `true`
- 시작 자리다 → `4`

**깜빡이는 발판 — 켜진 2초 안에 여덟 칸을 건넌다**

- 켜져 있다 → `true`
- 건넜다 → `true`
- _건너는 데 1.55초 (2초 안)_
- 2초 지나면 꺼진다 → `true`

**상자 — 걸어가서 밀고, 무거운 것은 혼자 못 민다 (같이 할 때)**

- 상자가 밀렸다 → `true`
- 미는 그림 → `true`
- 무거운 상자는 혼자 안 밀린다 → `true`
- 둘이 밀면 밀린다 → `true`
- _둘이 1초 밀어 1.57칸_

**상자 — 틈으로 밀면 떨어져 아래층에 앉는다**

- 2층에 있다 → `18`
- 1층 바닥에 앉았다 → `27`
- 틈 자리(52칸) 밑이다 → `true`

**색 열쇠 — 집으면 그 색 블록이 사라지고, 발밑이면 떨어진다**

- 빨간 블록이 막고 있다 → `true`
- 열쇠를 집었다 → `true`
- 빨간 블록이 사라졋다 → `true`
- 발밑이 사라져 복도로 떨어졋다 → `18`
- 파란 블록은 아직 있다 → `true`

**스위치와 누름판 — 스위치는 남고, 누름판은 밟는 동안만**

- 셔터 A 가 막고 있다 → `true`
- 스위치를 밟으면 열린다 → `true`
- 내려와도 열린 채 → `true`
- 셔터 P 가 막고 있다 → `true`
- 밟고 있으면 열린다 → `true`
- 떠나면 닫힌다 → `true`
- 상자가 눌러도 열린다 → `true`

**포탈 — 들어가면 저편에서 나온다, 상자도**

- 2층 20칸 근처로 나왔다 → `true`
- 상자도 저편으로 → `[20,47]`

**스프링 — 다섯 칸 튄다**

- _스프링 꼭대기 6.37칸_
- 다섯 칸쯤 튄다 → `true`

**왕복 발판 — 길을 오가고, 위에 서면 같이 간다**

- 첫 길은 20~40칸 → `[20,40]`
- 발판이 움직였다 → `true`
- 끝에서 되돌아온다 → `true`
- 발판 위에서 같이 움직였다 → `true`

**남의 머리 — 밟고 서고, 웅크린 사람은 낮다 (계단)**

- 머리 위에 섰다 → `true`
- 웅크리면 내가 내려온다 (계단) → `true`
- 머리 위에서 뛰면 세 칸을 넘는다 → `true`

**손잡기 — 세 칸 아래 사람을 끌어올린다**

- 끌어올리는 말을 보낸다 → `true`
- 당사자에게만 → `2`
- 끌려 올라갔다 → `53`
- 네 칸 아래는 못 잡는다 → `true`

**출구 — 「넷」 판은 넷이 다, 「한 명」 판은 하나면**

- 하나면 아직 → `false`
- 넷이면 된다 → `{"inside":4,"need":4,"ready":true}`
- 한 명 판은 하나면 된다 → `true`
- 넘어가기 시작 → `true`
- 다음 판이 열렸다 → `4`
- 시작 자리에 선다 → `18`

**마지막 판을 깨면 끝**

- 이겼다 → `true`

**시작 조건 — 넷이어야**

- 셋이면 막힌다 → `"넷이어야 시작한다 — 지금 1명"`
- 셋 → `"넷이어야 시작한다 — 지금 3명"`
- 넷이면 열린다 → `null`
- 다섯도 막힌다 → `"넷이어야 시작한다 — 지금 5명"`
- 혼자는 막힌다 (개발용 아니면) → `true`

**되감기 — 방장 ⌥R 이면 상자·열쇠·사람이 처음으로, 횟수를 센다**

- 열쇠를 집었다 → `true`
- 빨간 블록이 돌아왔다 → `true`
- 되감기 횟수 → `1`
- 같은 판이다 → `"두 열쇠"`

**시간 제한 — 마지막 판 120초**

- 120초 → `120`
- 다 되면 되감는다 → `true`

**통 — 구멍에서 나와 굴러가고 벽에 부딛히면 부서진다, 맞으면 밀려난다**

- 통이 나왔다 → `true`
- 왼쪽으로 굴러간다 → `true`
- 맞으면 넘어진다 → `true`
- 밀려난다 → `true`
- 죽지는 않는다 → `true`
- 벽(셔터·담)에 부딛혀 부서졌다 → `true`

**꾸러미 — 방장이 싼 것을 손님이 풀면 같은 판이 된다**

- 판 번호 → `9`
- 손님이 그 판을 연다 → `"3층 창고"`
- 시계 → `7.5`
- 열쇠 상태 → `true`
- 상자 자리가 따라온다 (멀면 그냥 옮긴다) → `true`
- 되감기 횟수 → `2`
- 깨진 꾸러미에도 안 터진다 → `true`

**손님 — 밀기와 출구는 방장에게 부탁한다**

- 밀기 부탁을 보낸다 → `true`
- 방장이 대신 민다 → `true`
- 출구 부탁 → `true`

**카메라 — 나를 따라가고 판 끝에서 멈춘다**

- 왼쪽 끝에서는 0 에 붙는다 → `0`
- 가운데쯤이면 나를 가운데 둔다 → `true`
- 오른쪽 끝에서는 판 끝에 붙는다 → `3024`
- 한 화면짜리 판은 안 움직인다 → `[0,0]`

